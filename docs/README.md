# Documentation index

Project overview and setup live in the **[root README](../README.md)**.

## Adding features / interviews

- **[FEATURE_MAP.md](FEATURE_MAP.md)** — where controllers, services, API clients, and pages live; minimal local run checklist.

## Guides (`guides/`)

Operational and historical notes live in **`guides/`** (deployment, email, tunnels, migrations, troubleshooting). Files keep their original names so bookmarks and searches still work.

| Topic | Examples (search in `guides/`) |
|--------|----------------------------------|
| Deploy / URLs | `DEPLOY_*`, `VERCEL_*`, `SMARTCART_URL_*`, `CLEAN_URL_*` |
| Email / Gmail / SES | `GMAIL_*`, `AWS_SES_*`, `FIX_GMAIL_*` |
| Sharing / tunnels | `NGROK_*`, `HOW_TO_SHARE`, `SHARING_*`, `CLOUDFLARE` (in script names) |
| Data / imports | `DEALS_IMPORT`, `HOW_TO_IMPORT_DEALS`, `*MIGRATION*` |
| Auth (legacy notes) | `JWT_AUTHENTICATION.md` |
| Project status | `PROJECT_*`, `NEXT_STEPS`, `START_HERE`, `SPEC_*` |

## Design / QA (`docs/` root)

- `motion-tokens.md`, `overview-motion-spec.md`, `performance-qa-checklist.md`

## Scripts

Shell automation is under **`../scripts/`** (see `../scripts/README.md`).

## Python utilities

Deal / data helpers are under **`../tools/`**.
